<Page
    x:Class="NstkWindowsApp.Views.PayloadPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                CornerRadius="8" Padding="16">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <Button x:ConnectionId='4' Content="Select Payload (.bin)"                             />
                <TextBlock x:ConnectionId='5' x:Name="PayloadNameText" Text="No file selected" VerticalAlignment="Center" Opacity="0.7" />
            </StackPanel>
        </Border>

        <Button x:ConnectionId='2' Grid.Row="1" x:Name="InjectButton" Content="Inject Payload"
                Style="{ThemeResource AccentButtonStyle}" HorizontalAlignment="Stretch"
                                     IsEnabled="False" />

        <Border Grid.Row="2" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="8">
            <ScrollViewer>
                <TextBlock x:ConnectionId='3' x:Name="LogText" Padding="8" FontFamily="Cascadia Mono, Consolas"
                           Text="Ready for RCM injection." TextWrapping="Wrap" />
            </ScrollViewer>
        </Border>
    </Grid>
</Page>

